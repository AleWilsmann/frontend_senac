export interface CarrinhoItem {
  id: string;
  nome: string;
  preco: number;
  descricao: string;
  quantidade: number;
}

export interface Produto {
  id: string;
  nome: string;
  preco: number;
  descricao: string;
  imagem: string;
}
